{
  "name": "TSB-Lab-Site",
  "version": "1.0.0",
  "description": "Website for the ideation and prototyping lab",
  "main": "build.js",
  "scripts": {
    "build": "node scripts/build.js && git add . && git commit -a -m \"build\" && git push && git checkout gh-pages && git rebase master && git filter-branch --prune-empty --subdirectory-filter site gh-pages",
    "boilerplate": "node scripts/boilerplate.js"
  },
  "devDependencies": {
    "fs": "0.0.1-security",
    "grunt": "^1.0.1",
    "grunt-contrib-sass": "^1.0.0",
    "grunt-contrib-uglify": "^3.1.0",
    "grunt-contrib-watch": "^1.0.0",
    "ncp": "^2.0.0"
  },
  "author": "Sebastian Meier @seb_meier"
}
